# python algorithm
======================================

print(, step=',') \n, ' ', '', ' , '
(end=' ') -> 줄바꿈

map(int, input()) -> 함수, 자료

실수 > 정수 -> 큰 범위 값으로

if문 조건 and, or, 0<x<10 가능
if, elif, else

for - else
for에서 정상종료 -> else, break -> else x

upper() 대문자, lower() 소문자

find(), count()
isupper(), isalpha()
ord() -> 아스키

a=[], a=list()
append(), insert(), remove(), index()

random.shuffle(), sort(오름차순), reverse=true(내림차순)
clear() 빈리스트

enumerate 튜플형식 반환 (인덱스, 값)
튜퓰은 값 변경 x, 리스트는 가능

all() 모두가 참 -> 참
any() 하나가 참 -> 참

함수에서 return 튜플형식으로도 가능

y=lambda x: x+1 -> 변수 할당 형식으로
내장함수 인자로 쓸 때 